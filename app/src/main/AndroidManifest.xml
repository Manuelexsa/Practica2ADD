<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kronos.practicaadd2" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Principal"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".JAnadir"
            android:label="@string/title_activity_janadir" >
        </activity>
        <activity
            android:name=".PJugador"
            android:label="@string/title_activity_pjugador" >
        </activity>
        <activity
            android:name=".PPartidos"
            android:label="@string/title_activity_ppartidos" >
        </activity>
    </application>

</manifest>
